export interface OrderProduct {
    id: string,
    originaName: string,
    name: string,
    quantity: number,
    totalAmount: number,
    originData: string,
    status: number,
    settings: string[]
}